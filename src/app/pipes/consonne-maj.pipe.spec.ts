import { ConsonneMajPipe } from './consonne-maj.pipe';

describe('ConsonneMajPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsonneMajPipe();
    expect(pipe).toBeTruthy();
  });
});
